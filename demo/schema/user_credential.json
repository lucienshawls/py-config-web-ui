{
    "title": "User Credential",
    "type": "object",
    "properties": {
        "username": {
            "title": "Username",
            "type": "string",
            "description": "Username",
            "minLength": 1
        },
        "password": {
            "title": "Password",
            "type": "string",
            "format": "password",
            "description": "Password must be at least 8 characters long.",
            "minLength": 8
        }
    },
    "required": [
        "username",
        "password"
    ]
}